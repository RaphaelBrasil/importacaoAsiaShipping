// project import
import pages from './pages'; //Auth
import dashboard from './dashboard';
import utilities from './utilities';
import support from './support';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [pages, dashboard, utilities, support]
};

export default menuItems;
